import styles from "./HeroScreen.module.scss";

export const HeroScreen = () => {
  return (
    <section className={styles.heroscreen}>
      <h1 className={styles.title}>Hello! It my project</h1>
    </section>
  );
};
